# v2 pre-RC code review (started 2026-09-23)

Tracks the code review that gates `v2.0.0-rc1` (Phase 6.6 in
[`v2-progress.md`](./v2-progress.md)). One section per round; each
finding gets a status and, when fixed, its commit.

## Why this scope

Dogfooding on 2026-09-23 found three bugs in about twenty minutes, all at
v1 ↔ v2 seams rather than in algorithms:

| Commit | Bug | Seam |
|---|---|---|
| `14e6a66b` | Translate page before inpaint → "Renderer ran but produced no output" | v2 engine hard-errors on a result the v1 facade returned silently |
| `fe09561c` | Click a text box after OCR → "Nothing to render" | UI auto-fires an RPC without its preconditions; `render` errors surface as a dialog |
| `8ace0884` | Text box handles float over the Welcome gate after Close project | Canvas z-indices not scoped to the canvas |

So the review is **diff-driven**: the unit of review is
`git diff origin/main...HEAD` (excluding `third-party/cudarc`) — about
13.3k added Rust lines in 83 files and 3.8k TS lines in 32 files, ~22% of
the codebase. From each changed entry point, trace outward into the v1
code it calls; v1 code with no v2 caller is out of scope (it shipped in
1.2.x).

## Method (every round)

1. Trace real code paths from the diff, not file by file. Every suspicion
   needs a concrete failure scenario.
2. Reproduce before fixing: a failing test, an e2e spec, or exact app
   steps. No repro → log it as *suspect*, do not fix.
3. One commit per bug, with a regression guard: Rust test, or an
   e2e spec for UI bugs (the UI has no unit-test setup). Gate:
   `cargo fmt --check`, `clippy --workspace --all-targets -D warnings`,
   `cargo test --workspace`; `tsc` clean in touched files.
4. Close the round: build, owner smoke-tests on the RTX 5070 box, record
   found / fixed / deferred below.

A round is done when every changed entry point in its scope has been
traced and every finding is fixed, deferred with a reason, or marked
not-a-bug.

## Rounds

| Round | Scope (v2 diff + the paths it reaches) | Diff size | RC gate |
|---|---|---|---|
| 0 | Run the e2e suite (`e2e/00`–`16`); fix meaningful `tsc` errors | — | yes |
| 1 | Project lifecycle, migration, blob store: `koharu-project`, `ops/project.rs`, V007 | ~0.8k + project.rs | yes |
| 2 | Engine seam: `koharu-pipeline`, `koharu-engines`, `koharu-core` | ~10k | yes |
| 3 | UI → RPC and UI state: `ui/lib`, `ui/hooks`, `ui/components` | ~3.8k | yes |
| 4 | Session / undo: `koharu-app` | ~1.7k | after RC ok |
| 5 | API / RPC / MCP / local HTTP server | ~0.4k | after RC ok |
| 6 | Small v2 touches: ml, renderer, types, app shell | ~0.5k | after RC ok |

Round 1 comes before the engine seam because migration is one-way and has
already run on a real project (TEST2); data loss cannot be undone, error
dialogs can.

## Known before the review

- `project_close` clears project + session but not the open documents;
  the closed project's pages stay loaded behind the Welcome gate.
  → Round 1.
- No log file under `%LOCALAPPDATA%\Koharu\` although the startup-failure
  dialog points there. → Round 6.
- `ui/lib/api.ts` has 110 of the 116 `tsc` errors (one schema/type
  mismatch pattern); `next build` skips type validation. → Round 0.

## Round 0 — e2e baseline + tsc

### Findings

| # | Finding | Status | Commit |
|---|---|---|---|
| 0.1 | AI chat auto-render read `e.call.args.index`; `ToolCall` only has JSON `arguments`, so it always re-rendered page 0 (edited page stale; untranslated page 0 → error dialog). Found by `tsc` TS2339. | fixed | `318ca295` |
| 0.2 | `text_renderer` bailed "Nothing to render" on a page with no blocks / no translations; v1 rendered the base page. Broke e2e/19 (inpaint → Render → export) and was the root of `fe09561c` and 0.1's dialog. | fixed | `b311b904` |
| 0.3 | #34 cache migration left `models/` untouched when `hf/` already had content → whole legacy cache stranded (16 GB here), everything re-downloaded. Triggered by `cargo test` creating `hf/` through hf_hub's fallback dir, or by one failed rename. Now merges per entry. **Shipped in v1.2.2 — replicate on main.** | fixed | `634f1844` |
| 0.4 | e2e suite could not reach the app: Welcome gate over the menu; specs started before the backend compiled / bound; font picker lost its test ids (`827023d0`); spec 15 relied on v1 minting a new image URL per render. | fixed (harness) | `29ea44ce`, `ff972961` |
| 0.5 | `RpcMethodMap` lacks 62 fork RPCs (110 of 116 `tsc` errors) — no compile-time check of fork RPC params/results. All 104 UI method names do exist in the Rust `Method` enum (scripted check). | deferred (after RC) | — |
| 0.6 | `api.saveDocuments` → `save_documents` has no Rust method and no caller. | not a bug (dead code) | — |
| 0.7 | `cloudLlm.ts:455` compares against `'none'` after that case already threw — unreachable message. | not a bug (dead branch) | — |
| 0.8 | e2e runs against the real `%LOCALAPPDATA%\Koharu` (engine profile, provider profiles, recent projects, model cache); a cloud OCR choice there fails 5 specs. There is no app-data override. | deferred — add `KOHARU_DATA_DIR`-style isolation | — |
| 0.9 | `cargo test` downloads models into the real app cache via hf_hub's fallback dir. | deferred → round 6 | — |

### e2e baseline (engine profile at defaults)

Pass: 00, 01, 10, 11, 13, 14, 15, 19.

Fail, environment: 12, 16, 17, 18 — `ensureLlmReady` allows 60 s, which
must cover the first download of the local LLM (vntl-llama3-8b Q8_0,
8.5 GB). On this box that file had never finished downloading (a
`.sync.part` in both `models/` and `hf/`); the owner translates with a
cloud profile. Not an app bug. Re-run once the model is fully cached.

The 0.3 merge was confirmed on real data: Hunyuan-MT and manga-ocr moved
from `models/` into `hf/`; duplicates stayed in `models/`.

**Round 0 closed 2026-09-23.** 3 app bugs fixed (0.1–0.3), e2e harness
repaired (0.4), 3 items deferred (0.5, 0.8, 0.9).

To run: temporarily move `%LOCALAPPDATA%\Koharu\engine_profile.json`
aside (restore afterwards) until 0.8 lands; stop any leftover
`next dev` on :3000 first, or Playwright reuses it without starting the
backend.
